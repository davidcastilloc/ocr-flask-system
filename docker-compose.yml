version: "3"
services:
  web:
    build:
      context: ./web/
    command: "gunicorn --workers 4 --name app -b 0.0.0.0:8000 --reload app.app:app"
    volumes:
      - ./web:/code:rw
    ports:
      - "8080:8000"
