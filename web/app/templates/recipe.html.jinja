{% extends "layout.html.jinja" %}
{% block content %}
<div class="container">
<div class="row">
    <div class="col-md-12">
    <div class="card">
        <div class="card-header">
        <h4 class="card-title">Recipe Medico</h4>
        <img class="rounded mx-auto d-block" src={{ url_for('static', filename='uploads/'+ img_filename) }} height="40%" width="40%">
    </div>
    </div>
    <div class="card">
    <div class="card-header">
        <h4 class="card-title">
        Selecciona el texto que quieres comparar
        </h4>
    </div>
    <div class="card-content m-3" >
        {% for item in recipe %}
        <button class="btn btn-primary">{{item}}</button>
        {% endfor %}
    </div>
    
    </div>
</div>
</div>
{% endblock %}